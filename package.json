{
  "name": "most-exclusive-website-3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rollup -c",
    "autobuild": "rollup -c -w",
    "dev": "run-p start:dev autobuild",
    "start": "sirv public --single",
    "start:dev": "sirv public --single --dev",
    "build:test": "cp -r ./public/* ./dist; npm run build;",
    "deploy:test": "cp -r ./public/* ./dist; npm run build; netlify deploy --dir ./dist;",
    "deploy:prod": "cp -r ./public/* ./dist; npm run build; netlify deploy --prod --dir ./dist;"
  },
  "devDependencies": {
    "sirv-cli": "^0.4.5",
    "@fullhuman/postcss-purgecss": "^1.3.0",
    "autoprefixer": "^9.6.5",
    "cssnano": "^4.1.10",
    "npm-run-all": "^4.1.5",
    "postcss-import": "^12.0.1",
    "rollup": "^1.25.2",
    "rollup-plugin-commonjs": "^10.0.0",
    "rollup-plugin-livereload": "^1.0.0",
    "rollup-plugin-node-resolve": "^5.2.0",
    "rollup-plugin-postcss": "^2.0.3",
    "rollup-plugin-svelte": "^5.0.3",
    "rollup-plugin-terser": "^4.0.4",
    "svelte": "^3.0.0",
    "svelte-spa-router": "^1.2.1",
    "tailwindcss": "^1.1.3"
  },
  "dependencies": {
    "proton-engine": "^4.0.1"
  }
}
